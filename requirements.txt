# Runtime dependencies for the Docker build (mirrors pyproject [project].dependencies).
# Core BACnet simulator + REST/SSE API + TimescaleDB historian + forecast (naive fallback).
BAC0>=24.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.0
aiosqlite>=0.20.0
httpx>=0.27.0
pyyaml>=6.0
asyncpg>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12
pymodbus>=3.8.0
xknx>=2.0.0  # KNX/IP communication (lazy-imported; engine no-ops if absent)
